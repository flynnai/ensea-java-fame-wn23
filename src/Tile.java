import javafx.scene.image.ImageView;

public class Tile {
    public int tilesetRow;
    public int tilesetCol;
    public Tile(int tilesetRow, int tilesetCol) {
        this.tilesetRow = tilesetRow;
        this.tilesetCol = tilesetCol;
    }
}
